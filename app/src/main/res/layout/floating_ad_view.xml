<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/floating_ad_container"
    android:layout_width="200dp"
    android:layout_height="150dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="6dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Ad content container -->
        <FrameLayout
            android:id="@+id/ad_content_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <!-- This is where you'll place your ad view -->
            <TextView
                android:id="@+id/ad_placeholder"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:gravity="center"
                android:text="Ad Content Here"
                android:textSize="16sp" />
        </FrameLayout>

        <!-- Close button -->
        <ImageButton
            android:id="@+id/btn_close_ad"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentTop="true"
            android:background="@android:drawable/ic_menu_close_clear_cancel"
            android:contentDescription="Close ad" />
    </RelativeLayout>
</androidx.cardview.widget.CardView>